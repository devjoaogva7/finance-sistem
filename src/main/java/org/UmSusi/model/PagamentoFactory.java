package org.UmSusi.model;

import org.UmSusi.interfaces.FormaPagamento;
import org.UmSusi.model.enuns.EnumFormaPagamento;

public class PagamentoFactory {
//    public static FormaPagamento criarPagamento(EnumFormaPagamento tipo, String... parametros) {
//        switch (tipo) {
//            case PIX:
//                if (parametros.length > 0) {
//                    return new PagamentoPix(parametros[0]);
//                }
//                throw new IllegalArgumentException("Chave PIX é obrigatória");
//            case CARTAO_CREDITO:
//            case CARTAO_DEBITO:
//                throw new UnsupportedOperationException("Implementação pendente para " + tipo.getDescricao());
//            default:
//                throw new IllegalArgumentException("Tipo de pagamento não suportado: " + tipo);
//        }
//    }
} 